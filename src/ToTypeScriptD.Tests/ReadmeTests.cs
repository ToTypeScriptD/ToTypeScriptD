using System;
using System.Linq;
using ToTypeScriptD.Tests.ExeTests;
using Xunit;

namespace ToTypeScriptD.Tests
{
    public class ReadmeTests
    {
//        [Fact]
//        public void ReadmeShouldContainCommandLineHelp()
//        {
//            var readme = System.IO.File.ReadAllText("../../../../Readme.md");
//            var startToken = "```CommandLine" + Environment.NewLine;
//            var endToken = "```" + Environment.NewLine;
//            var header = @"*******
//*******
//**** IF THIS CHANGES - Update the Readme when this version is published ***
//*******
//*******" + Environment.NewLine + Environment.NewLine;

//            var readmeHelp = readme.Substring(readme.IndexOf(startToken) + startToken.Length);
//            readmeHelp = readmeHelp.Substring(0, readmeHelp.IndexOf(endToken));

//            var options = new Options();
//            var usage = options.GetUsage();
//            // take out the version and copyright
//            usage = usage.Split(new string[] { Environment.NewLine }, StringSplitOptions.None).Skip(3).Join(Environment.NewLine);

//            var usageFinal = header + usage.Trim();
//            var readmeFinal= header + readmeHelp.Trim();

//            (usageFinal).DiffWith(readmeFinal);
//        }
    }
}
